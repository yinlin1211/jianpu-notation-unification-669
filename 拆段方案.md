# 拆段方案:LRC 句 → 段级 json + 段级 SVG

> 目标:按 LRC 句(两句一段)把整页谱面拆成"段对象" = 段级 json(数据)+ 段级 SVG(视图),
> 两边用同一套主键逐音符对齐。

## 一、数据主键(严谨拆法的核心)

谱面的"数据本尊"是 `0830/songs_unified/{song}.json`(669 首全量素材库),SVG 是它渲染出来的"影子":

```
songs_unified/{song}.json          苍强解析器(hc_*.js)           SVG
notes[] 结构化 cell 流  ─────────────────────────────►  视觉谱面
   (渲染器输入, noteCount=N)                       (svg_note_count=N)
```

- `notes[i]` ↔ SVG 第 i 个 `<use>` 音符元素,**顺序一一对应**(牵丝戏 200 = 200)
- 每个音符已结构化:`{content, octave, underBarCount, dotCount, durationRatio, noteNumber(midi)}`
  —— **方言在字段层已归一**(`underBarCount:2` 就是两条减时线,不管原文写 `xx`/`__`/`=`)
- json 无坐标、无歌词;SVG 有 x/y、firstLine/firstColumn、逐字歌词 `<text>`
- 歌词挂载规律(牵丝戏实测):`x_lyric = x_note − 13`、`y_lyric = y_note + 44`;
  歌词 text **成对出现**(相差 36px)= 多遍演唱的两段词各一行

## 二、拆解流程

```
① 两句 LRC 进来
      ↓ 词序匹配(⚠️不能用 LRC 时间戳: LRC=实际音频时间轴, 谱面=bpm 合成时间轴, 对不上)
② 在播放展开流里定位 → 这两句的音符集合 loc={(line,col,occ)} + onset/offset
      ↓ 以 loc 为键回 songs_unified / 统一简谱 json 拆三样
③ 拆 json(母体 → 段级子集)
      ├─ 音符子集  notes 切片(midi/时值/jianpu)
      ├─ 字槽子集  每音符挂的词(含各遍 occ 的词)
      └─ 段级时间  起点=句1首音 onset, 终点=句2末音 offset
④ 拆 SVG(视图)
      ├─ 音符: <use> 的 firstLine/firstColumn = loc → 直接筛
      ├─ 歌词: <text> 按 x±容差 + y 行带挂回音符
      └─ 裁切: 段内元素 y 外接框 → viewBox 子框
⑤ 产出段对象
   { lrc:[两句原文+时间戳], notes:[{loc,midi,onset,dur,jianpu,chars}], svg:裁切图 }
```

## 三、判定"两句能成一段"的三个连续条件

| 条件 | 判据 | 牵丝戏实测例 |
|---|---|---|
| 词序连续 | 句 i+1 的词在演唱流里紧跟句 i | 句9→句10 连续 |
| 版面连续 | 两句音符谱面行差 ≤1(SVG 连续流水排版) | 句9/句10 同在 y=588 视觉线 |
| 播放连续 | 前句末音 offset = 后句首音 onset(零间隔) | "水"offset 45.83s = "你"onset 45.83s |

中间夹**间奏行**(无词)→ 天然段界,不硬切。

## 四、兜底规则(牵丝戏 33 句全量验证)

| 情况 | 数量 | 处理 |
|---|---|---|
| 元数据行(制作人/作词/作曲) | 3 | 排除 |
| 正常定位、句对"隔壁" | **17 对通过** | 直接成段 |
| 跨间奏断点 | 1(句7→8, 隔行19 间奏) | 间奏处切分 |
| 谱面缺词(LRC 有、谱面无) | 4(`兰花指捻红尘似水`等) | 挂时间最近音符+标记"补挂" |
| 尾声重复句 | 4(句30-33) | 按第 n 次出现 → 第 n 遍播放匹配 |
| 多段词歌 | 来迟等 227 首 | loc 相同 occ 不同, 按遍选词 |

## 五、案例:牵丝戏 句9+句10

```
句9  你褴褛我彩绘 并肩行过山与水   谱面行20-22 → SVG 同一视觉线 y=588 (x 195→907)
句10 你憔悴 我替你明媚             行22-23 → y=588 末尾折到 y=741 (x 65→466)
段内 27 音符, loc→SVG 坐标全部对上, "水"offset=45.83s 与 "你"onset=45.83s 零间隔咬合
```

## 六、注意事项

1. **LRC 时间戳与谱面时间轴无关**(牵丝戏 LRC 到 218.8s,谱面合成流止于 184s)——定位必须用词序
2. 谱面词可能不完整(谱面词 267 字 < LRC 全词)
3. SVG 里歌词 text 无谱面坐标属性,按 x 对齐挂回
4. 多段词的演唱遍 occ 维度必须保留,否则第二三遍唱哪段词会错
